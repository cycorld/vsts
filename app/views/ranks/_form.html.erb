<%= form_for(rank) do |f| %>
  <% if rank.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rank.errors.count, "error") %> prohibited this rank from being saved:</h2>

      <ul>
      <% rank.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :score %>
    <%= f.number_field :score %>
  </div>

  <div class="field">
    <%= f.label :is_visible %>
    <%= f.check_box :is_visible %>
  </div>

  <div class="field">
    <%= f.label :abusing_cnt %>
    <%= f.number_field :abusing_cnt %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
